component ddt_bit "Comprueba variacion de un flotante de entrada";
pin in float in_float;
pin out bit out_bit;
variable float old;
function _;
license "GPL";
description "Devuelve True (1)  si las entradas no varian un 10%"; 
;;
float tmp = in_float;
out_bit = 0;
if (abs((tmp - old) / fperiod) < 0.1) out_bit= 1 ;
old = tmp;
